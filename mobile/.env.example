# Copy to `.env` (gitignored). Without these, the app runs in local mode.
# Supabase → Project Settings → API. The anon key is safe in a client (RLS-protected).
EXPO_PUBLIC_SUPABASE_URL=https://fqdemnuhyvygkdookwbr.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
